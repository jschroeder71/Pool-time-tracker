{
  "name": "pool-time-tracker",
  "version": "2.0.0",
  "private": true,
  "description": "Field time tracking PWA for pool service technicians",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "VITE_BASE=/pool-time-tracker/ vite build && gh-pages -d dist",
    "deploy:custom": "vite build && gh-pages -d dist"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "@supabase/supabase-js": "^2.45.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "gh-pages": "^6.1.1",
    "vite": "^5.4.2"
  }
}
