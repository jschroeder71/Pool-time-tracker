name: Deploy to GitHub Pages

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 18
      - run: npm install
      - run: npm run build
        env:
          VITE_BASE: /Pool-time-tracker/
         VITE_SUPABASE_URL: https://qkbwmnkuljsbttdqdhww.supabase.co
          VITE_SUPABASE_ANON_KEY: sb_publishable_U8PG57af9kIKc5DQCkYIxA_DaUJV_Ri

      - uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
